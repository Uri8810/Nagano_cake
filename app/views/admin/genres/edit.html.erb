<%= form_with model: @genre, url: admin_genre_path, local: true do |f| %>
<h2>ジャンル編集</h2>
<div class="form-group">
  	<label for="genre-name">ジャンル名</label>
    <%= f.text_field :name, id:"genre-name", class:"form-control" %>
 </div>
 <%= f.submit "変更を保存"%>
 <% end %>